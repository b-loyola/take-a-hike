<tr class="hike-row" data-name="<%= hike.name %>" data-distance="<%= hike.distance_in_km %>">
  <td><%= link_to hike.name, hike_path(hike) %></td>
  <td><%= pluralize(hike.distance_in_km, 'Km') %></td>
  <td><%= pluralize(hike.time_in_hours, 'Hour') %></td>
  <td><%= hike.difficulty_description %></td>
  <!-- <td><%= hike.description %></td> -->
<tr>
